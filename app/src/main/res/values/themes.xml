<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Uniandroiddev3" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colors -->
        <item name="android:textColor">@color/green</item>
        <item name="android:background">@color/dark_green</item>
        <item name="android:textColorHint">@color/pale_green</item>
        <!-- Font stuff -->
        <item name="android:fontFamily">monospace</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="Theme.Uniandroiddev3" parent="Base.Theme.Uniandroiddev3" />
</resources>